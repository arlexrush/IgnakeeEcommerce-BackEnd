# 01-upgrade-project-targeting: Align the solution to .NET 8

Update all project files to target .NET 8 and align the package references with the new target framework. This task covers the SDK/project metadata and the package set needed by the API, application, domain, and infrastructure layers.

**Done when**: All projects target net8.0, package references are compatible with .NET 8, and the solution restores successfully.
